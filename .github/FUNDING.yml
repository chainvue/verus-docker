# GitHub Sponsors and friends.
#
# Nothing is enabled: this project has no funding channel set up, and shipping
# a placeholder address that looks real would be worse than shipping nothing.
#
# To enable, uncomment ONE of the following and fill it in. `custom` takes URLs
# rather than raw addresses, so a VRSC or VerusID donation address belongs on a
# page you control that the URL points at.
#
# github: [devdudeio]
# custom: ["https://example.org/donate"]
